#include <iostream>
#include <vector>
using namespace std;

void solve(string digits,
           string output,
           int index,
           vector<string> &ans)
{
    if (index == digits.length())
    {
        ans.push_back(output);
        return;
    }
}

int main()
{

    return 0;
}